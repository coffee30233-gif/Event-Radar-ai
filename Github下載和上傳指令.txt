##用 CMD 或 PowerShell 都可以直接跑這個：
robocopy "C:\Users\lynn.yeh\Downloads\event-radar-ai" "D:\Event Radar" /E /XF events.json manual_events.json .env.local /XD .git node_modules

##跑完之後一樣進到你的 repo 資料夾，用 git 指令檢查：
cd "D:\Event Radar"
git status
git diff
git add .
git commit -m "sync updates from this round"
git push